\documentclass[aspectratio=169,10pt]{beamer}
\usetheme{Madrid}
\usepackage{graphicx}
\usepackage{amssymb}

% Loại bỏ bóng đổ giao diện phẳng
\setbeamertemplate{blocks}[rounded][shadow=false]
\setbeamertemplate{navigation symbols}{}

% Color configuration
\definecolor{primaryblue}{RGB}{20, 55, 110}
\setbeamercolor{palette primary}{bg=primaryblue,fg=white}
\setbeamercolor{structure}{fg=primaryblue}

\begin{document}

\begin{frame}{Formal Definition: Function of Two Variables}
    \begin{columns}[T]
        % Cột trái: Khung định nghĩa toán học
        \begin{column}{0.54\textwidth}
            \begin{block}{Definition (Function of Two Variables)}
                \small
                Let $z = f(x, y)$ be a function with two variables $x$ and $y$.
                \begin{itemize}
                    \item $x$ and $y$ are the \textbf{independent variables}.
                    \item $z$ is the \textbf{dependent variable}.
                    \item Unless otherwise specified, the \textbf{domain} $D$ is the \textbf{largest subset of $\mathbb{R}^2$} on which $f$ is mathematically well-defined.
                \end{itemize}
            \end{block}
            
            \vspace{0.1cm}
            \begin{exampleblock}{Geometric Mapping}
                \small
                \[
                    f: D \subseteq \mathbb{R}^2 \longrightarrow \mathbb{R}, \quad (x, y) \longmapsto z
                \]
            \end{exampleblock}
        \end{column}

        % Cột phải: Animation 3D mô phỏng phép chiếu miền xác định lên mặt cong
        \begin{column}{0.44\textwidth}
            \centering
            \includegraphics[width=0.88\linewidth, keepaspectratio]{FunctionMappingScene.png}
            
            \vspace{0.1cm}
            {\tiny \textcolor{gray}{\textit{Input $(x, y) \in D$ maps vertically to Output $z = f(x,y)$}}}
        \end{column}
    \end{columns}
\end{frame}

\end{document}
